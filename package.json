{
  "name": "ironband",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "暗黑破坏神2 风格手游 — 数据驱动 Web/TS 重构 (单机, 等距 ARPG)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:site": "npm run build && cp dist/web/index.html index.html",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^2.1.8"
  }
}
