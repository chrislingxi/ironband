{
  "name": "ironband",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "暗黑破坏神2 风格手游 — 数据驱动 Web/TS 重构 (单机, 等距 ARPG)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:site": "npm run build && cp dist/web/index.html index.html",
    "assets:v4-boss-placeholders": "node scripts/gen-v4-bosses.mjs",
    "assets:v4-tiles": "node scripts/gen-v4-tiles.mjs",
    "assets:v4-barbarian-icons": "node scripts/gen-v4-barbarian-icons.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "selftest:lint": "node scripts/selftest-lint.mjs",
    "selftest:ui": "node scripts/selftest-ui.mjs",
    "selftest:mobile-zoom": "node scripts/selftest-mobile-zoom.mjs",
    "selftest": "npm run selftest:lint && npm run typecheck && npm test && npm run build && npm run selftest:ui && npm run selftest:mobile-zoom",
    "loop:check": "npm run selftest:lint && npm run typecheck && npm test && npm run build && npm run selftest:mobile-zoom"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^2.1.8"
  }
}
