# V4 Runtime Masters

This directory preserves the full-resolution source art for player portraits, late-act Bosses, and large camp props.

The matching files under `assets/` and `public/assets/` are mobile delivery copies capped at a 768px longest edge. Keep both runtime trees byte-identical and retain the source file here before any future resize or recompression pass.
